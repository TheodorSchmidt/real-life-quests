import React from "react";
import { observer } from "mobx-react-lite";
import { createUseStyles } from "react-jss";

function TavernPurchases() {
    return(
        <div>Какая-то штука</div>
    )
}

export default observer(TavernPurchases);