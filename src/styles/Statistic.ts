import { createUseStyles } from 'react-jss';

export const statisticStyle = createUseStyles({
    chart: {
        width: '100%',
        height: '100%'
    },
    headline: {
        fontSize: '22px',
        fontWeight: 'bold',
        marginLeft: '20px',
        marginTop: "20px"
    }
})